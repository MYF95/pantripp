- provide(:title, 'Users')

%div.jumbotron
  %h1 Here are all the users in this list
  %h2= "This list has #{pluralize(@list.users.count, 'user')}"
  %ul.userlist
    - @list.users.each do |user|
      %li= link_to "#{user.name}", user_path(user)