- provide(:title, 'Edit list')
- provide(:button_text, 'Update list')

.jumbotron
  .container
    .col-md-6.col-md-offset-3
      %h2 Create new list
      = form_for(@list) do |f|
        = render 'form', f: f